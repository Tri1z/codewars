function descendingOrder(n){
  return parseInt(
    n.toString()       // превращаем число в строку, например 42145 → "42145"
     .split('')        // разбиваем строку в массив символов: ["4","2","1","4","5"]
     .sort((a, b) => b - a) // сортируем по убыванию: ["5","4","4","2","1"]
     .join('')         // соединяем обратно в строку: "54421"
  )
  //...
}